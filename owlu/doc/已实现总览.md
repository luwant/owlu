# 已实现总览（截至 2026-03-11）

## 架构概述

OWLU (Open World Label Updating) 采用**三模块闭环架构**:

```
Discovery ──MatchResult──▶ Writer ──promoted_labels──▶ Absorption
    ▲                                                       │
    └───────────── 更新后的 model_state (E, P, threshold) ◀─┘
```

| 模块 | 职责 | 文档 |
|------|------|------|
| **Candidate Discovery** | 从低置信样本发现候选标签 | [candidate_discovery.md](candidate_discovery.md) |
| **Ontology-Constrained Writer** | 跨文档积累 + 本体约束晋升 | [ontology_writer.md](ontology_writer.md) |
| **Prototype Absorption** | 将新标签吸收到 LTC-MPE 表示空间 | [prototype_absorption.md](prototype_absorption.md) |

---

## 目录结构

```
owlu/
├── __init__.py              # 包入口 + 向后兼容重导出
├── configs/
│   └── owlu.yaml            # 统一配置文件
├── common/                  # 共享层
│   ├── __init__.py
│   └── types.py             # OWLUConfig, CandidatePhrase, MatchResult, ...
├── discovery/               # Module 1: Candidate Discovery
│   ├── __init__.py          # CandidateDiscovery (Facade)
│   ├── gate.py              # LtceGate — 置信度门控
│   ├── phrase_generator.py  # LLMPhraseGenerator — DeepSeek 短语抽取
│   └── matcher.py           # SemanticMatcher — BOW 语义匹配
├── writer/                  # Module 2: Ontology-Constrained Writer
│   ├── __init__.py          # OntologyWriter (Facade)
│   ├── label_bank.py        # LabelBank — 跨文档积累与晋升
│   └── constraints.py       # OntologyConstraintChecker — 本体约束
├── absorption/              # Module 3: Prototype Absorption
│   ├── __init__.py          # PrototypeAbsorption (Facade)
│   ├── metrics.py           # 向量数学、评分、阈值校准
│   ├── fast_sync.py         # 语义刷新 (标签数不变)
│   └── slow_sync.py         # 标签扩维 L→L' + 增量微调
├── doc/
│   ├── 已实现总览.md         # 本文件
│   ├── candidate_discovery.md
│   ├── ontology_writer.md
│   └── prototype_absorption.md
└── tests/
```

---

## Module 1: Candidate Discovery

从"模型目前解释不好的样本"里，发现值得进入闭环体系审核的候选标签。

- **LtceGate** (`discovery/gate.py`):
  基于 `raw_max_prob`（max sigmoid at T=1.0）判定 OOD，自适应校准（验证集第 5 百分位 + floor=0.10）。
- **LLMPhraseGenerator** (`discovery/phrase_generator.py`):
  DeepSeek 兼容 API 调用 + JSON 容错解析 + 多采样一致性投票（k=3, agreement）。
- **SemanticMatcher** (`discovery/matcher.py`):
  BOW cosine 轻量匹配，三路初判（merge_pre / novel_pre / hold_pre）。
- **CandidateDiscovery** (`discovery/__init__.py`):
  Facade，一步完成 gate→extract→match。

**输出**: `list[MatchResult]` → 送入 Writer。

详见 [candidate_discovery.md](candidate_discovery.md)。

---

## Module 2: Ontology-Constrained Writer

把候选标签写入标签体系（以 AAPD 为例），在写入前进行本体约束检查。

- **LabelBank** (`writer/label_bank.py`):
  状态机 hold → candidate → promoted，跨文档簇聚合（freq ≥ 3, source_docs ≥ 2）。
- **OntologyConstraintChecker** (`writer/constraints.py`):
  命名格式校验 + 父类存在性检查 + 重复标签拒绝。提供 `for_aapd()` 工厂方法。
- **OntologyWriter** (`writer/__init__.py`):
  Facade，ingest + promote + 标签清单导出。

**输出**: `promoted_labels` + `LabelBank` 实例 → 送入 Absorption。

详见 [ontology_writer.md](ontology_writer.md)。

---

## Module 3: Prototype Absorption

把已经被闭环体系批准的新标签，真正吸收到 LTC-MPE 的表示空间里。

- **metrics.py** (`absorption/metrics.py`):
  向量运算、score_document、infer_topk、Macro-F1 grid search 阈值校准。
- **fast_sync** (`absorption/fast_sync.py`):
  别名语义混合 E/P（η_e=0.2, η_p=0.1）+ 阈值重标定，标签数不变。
- **slow_sync** (`absorption/slow_sync.py`):
  标签扩维 L→L'，新标签初始化 + 一次增量微调 + 阈值重标定。
- **PrototypeAbsorption** (`absorption/__init__.py`):
  Facade，`fast_absorb()` / `slow_absorb()` 两种吸收策略。

**输出**: 更新后的 `model_state {E, P, threshold}` → 反馈给 Discovery。

详见 [prototype_absorption.md](prototype_absorption.md)。

---

## 共享数据结构 (common/types.py)

| 结构 | 说明 |
|------|------|
| `OWLUConfig` | 统一配置，从 owlu.yaml 加载（含 LLM / 阈值 / 门控 / 采样参数） |
| `CandidatePhrase` | LLM 产出的候选短语（text, agreement, source_doc_id, ...） |
| `MatchResult` | 语义匹配结果（action, similarity, target_label） |
| `LabelInfo` | 标签元数据（aliases, description） |
| `ProtoLabelCluster` | 候选簇（phrases, freq, source_docs, state） |
| `GateDecision` | 门控决策（should_invoke_llm, raw_max_prob） |
| `ValidationSample` | 验证样本（embedding, true_labels） |

---

## 向后兼容

`owlu/__init__.py` 提供旧路径的重导出：

```python
# 旧代码仍然可用：
from owlu import LabelBank, LtceGate, fast_sync, OWLUConfig
# 等价于：
from owlu.writer.label_bank import LabelBank
from owlu.discovery.gate import LtceGate
from owlu.absorption.fast_sync import fast_sync
from owlu.common.types import OWLUConfig
```
