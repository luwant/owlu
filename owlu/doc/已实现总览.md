# 已实现总览（截至 2026-03-10）

## 1. 任务1：OWLU 抽取与门控

- 已实现 DeepSeek 兼容调用与 JSON 解析（含异常兜底）。
- 已实现候选短语抽取、低置信触发、多采样一致性聚合（agreement）。
- 已实现语义匹配与初判逻辑：`merge_pre / novel_pre / hold_pre`。
- 已完成任务1单测与 live API 集成测试（环境变量开启时）。

核心文件：
- `owlu/llm_phrase_generator.py`
- `owlu/semantic_matcher.py`
- `owlu/tests/test_task1.py`
- `owlu/tests/test_task1_live_api.py`

---

## 2. 任务2：标签库与快同步

- 已实现 LabelBank 状态流转：`hold / candidate / promoted`。
- 已实现簇聚合、审核包、晋升流程（`promoted_labels`）。
- 已实现 `fast_sync`：固定维度下更新 `E/P`，并重标定阈值。
- 已完成任务2单测、LTCE 集成测试、OOD 对比测试。

核心文件：
- `owlu/label_bank.py`
- `owlu/ltc_sync.py`（`fast_sync`）
- `owlu/ood_eval.py`
- `owlu/tests/test_task2.py`
- `owlu/tests/test_task2_ltce_integration.py`
- `owlu/tests/test_task2_ood_compare.py`

---

## 3. 任务3：慢同步与端到端评测

- 已实现 `slow_sync` 主流程：
  - 从 `LabelBank.promoted_labels` 读取晋升标签
  - `L -> L'` 扩维与模型状态重建
  - 旧样本回放 + 新标签样本增量更新
  - 带 `P@3` 约束的阈值重标定
  - 输出可追溯 `sync_report`
- 已实现 SQLite 最小可追溯存储（run 记录、标签快照、晋升记录、e2e 报告）。
- 已实现端到端评测与 3 组消融（去一致性门控、去不确定性回访、去候选簇晋升）。
- 已新增 LTCE 衔接烟测入口（`Label-gen` 路径）。
- 已完成任务3单测与 LTCE 集成烟测（环境变量开启时）。

核心文件：
- `owlu/ltc_sync.py`（`slow_sync`）
- `owlu/task3_sqlite.py`
- `owlu/task3_eval.py`
- `owlu/scripts/init_task3_sqlite.py`
- `owlu/tests/test_task3.py`
- `owlu/tests/test_task3_ltce_integration.py`

---

## 4. 当前测试状态

- 全量：`pytest -q owlu/tests`
- 最新结果：`33 passed`
